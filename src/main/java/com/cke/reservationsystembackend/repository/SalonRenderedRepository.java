package com.cke.reservationsystembackend.repository;

import com.cke.reservationsystembackend.model.SalonRendered;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SalonRenderedRepository extends JpaRepository<SalonRendered,Long> {
}
